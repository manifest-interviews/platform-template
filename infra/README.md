# infra/

Infrastructure-as-code for the Booking API lives here as a Pulumi TypeScript
project (`pulumi/`).

It is currently empty: nothing is defined and nothing is deployed. Today the
service only runs via the local `docker compose` stack.
